import 'package:flutter/material.dart';

class DatabaseLoadScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Database Load'),
      ),
      body: Center(
        child: Text('Database Load Screen'),
      ),
    );
  }
}
