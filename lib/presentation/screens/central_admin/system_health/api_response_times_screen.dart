import 'package:flutter/material.dart';

class ApiResponseTimesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('API Response Times'),
      ),
      body: Center(
        child: Text('API Response Times Screen'),
      ),
    );
  }
}
