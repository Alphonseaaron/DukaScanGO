import 'package:flutter/material.dart';

class ServerPerformanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Server Performance'),
      ),
      body: Center(
        child: Text('Server Performance Screen'),
      ),
    );
  }
}
