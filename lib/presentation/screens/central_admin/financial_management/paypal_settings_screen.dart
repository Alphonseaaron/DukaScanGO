import 'package:flutter/material.dart';

class PayPalSettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PayPal Settings'),
      ),
      body: Center(
        child: Text('PayPal Settings Screen'),
      ),
    );
  }
}
