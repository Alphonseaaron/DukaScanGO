import 'package:flutter/material.dart';

class StorePayoutsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Store Payouts'),
      ),
      body: Center(
        child: Text('Store Payouts Screen'),
      ),
    );
  }
}
