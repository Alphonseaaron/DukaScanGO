import 'package:flutter/material.dart';

class AgentPayoutsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Agent Payouts'),
      ),
      body: Center(
        child: Text('Agent Payouts Screen'),
      ),
    );
  }
}
