import 'package:flutter/material.dart';

class StripeSettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stripe Settings'),
      ),
      body: Center(
        child: Text('Stripe Settings Screen'),
      ),
    );
  }
}
