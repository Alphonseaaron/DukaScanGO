import 'package:flutter/material.dart';

class PerformanceBonusesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Performance Bonuses'),
      ),
      body: Center(
        child: Text('Performance Bonuses Screen'),
      ),
    );
  }
}
